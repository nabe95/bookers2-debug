<%= form_with model:[book,book_comment], local: false do |f| %>
    <%= f.text_area :comment, rows: '5',placeholder: "コメントをここに", cols: 100, class:"comment-texrarea" %><br>
    <%= f.submit "送信" %>
<% end %>